
[package]
name = "iml-bench"
version = "0.1.0"
edition = "2021"

[dependencies]
core = { path = "../core" }
checker = { path = "../checker" }
wasm = { path = "../wasm" }
syntax = { path = "../syntax" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
wasmtime = "25.0"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "iml_benchmark"
harness = false

